import 'package:flutter/material.dart';

class SymptomDetail extends StatelessWidget {
  const SymptomDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
